<%= render "profile" %>
<div class="container">
  <div class="book_review_boxs">
    <% @reviews.each do |review| %>
      <%= render "partial/book_review", review: review %>
    <% end %>
  </div>
  <%= paginate @reviews %>
</div>