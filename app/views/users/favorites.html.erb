<%= render "profile" %>
<div class="container">
  <div class="book_review_boxs">
    <% @favorites.each do |review| %>
      <%= render "partial/book_review", review: review %>
    <% end %>
  </div>
  <%= paginate @favorites %>
</div>